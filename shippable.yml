build:
  ci:
    - docker build -t quay.io/bobhenkel/hello:shippable-$BRANCH.BUILD_NUMBER .
    
  post_ci:
    - docker push quay.io/bobhenkel/docker-hello:shippable-$BRANCH.BUILD_NUMBER
  
  integrations:
  hub:
    - integrationName: quay-integration    #replace with your integration name
      type: quayLogin
